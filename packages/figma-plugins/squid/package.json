{
  "name": "@opendesign/figma-plugin-squid",
  "version": "3.0.0-rc.6",
  "description": "Figma Plugin Squid",
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rimraf ./node_modules ./lib ./dist",
    "lint": "echo 'Linting is not implemented yet'",
    "test": "echo 'Tests are not implemented yet'",
    "tsc": "yarn tsc:main && yarn tsc:ui",
    "tsc:main": "tsc --noEmit -p src/main",
    "tsc:ui": "tsc --noEmit -p src/ui",
    "tsc:watch": "concurrently -n widget,iframe \"yarn tsc:main -- --watch --preserveWatchOutput\" \"yarn tsc:ui -- --watch --preserveWatchOutput\"",
    "build": "yarn build:ui && yarn build:main",
    "build:main": "esbuild src/main/index.ts --bundle --outfile=dist/main.js",
    "build:ui": "vite build --minify esbuild --emptyOutDir=false",
    "build:watch": "concurrently -n widget,iframe \"yarn build:main -- --watch\" \"yarn build:ui -- --watch\"",
    "dev": "concurrently -n tsc,build,vite 'yarn:tsc:watch' 'yarn:build:watch' 'vite'"
  },
  "dependencies": {
    "@opendesign/figma-plugin-common": "3.0.0-rc.8",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "1.63.0",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.3.1",
    "concurrently": "^6.3.0",
    "esbuild": "0.15.12",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "typescript": "4.8.4",
    "vite": "^2.5.2",
    "vite-plugin-singlefile": "^0.5.1",
    "vite-svg-loader": "^3.3.0"
  },
  "files": [
    "dist"
  ],
  "main": "dist/main.js"
}
